spring:
  application:
    name: test

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always

  mvc:
    hidden method:
      filter:
        enabled: true
